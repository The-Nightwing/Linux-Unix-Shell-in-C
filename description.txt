UNIVERSAL ERROR:
    - If the user give a command other than the 10 commands given in the assignment, the program 
    will show command not found.

ls:
    flags used: ls -1, ls -a
    Errors:
        - None

echo:
    flags used: -n, -E 
    Errors: None

rm:
    flags used: -r, -f
    Errors Handled:
        - if no file/dir is given after rm it throws an error: missing operand.
        
cat:
    flags used: -n, -E
    Errors handled:
        - If file does not exits it will throw an error: no such file or directory

history:
    flags used: -a, -c

mkdir:
    flags used: --help, -v
    Errors Handled:
        - if no file/dir is given after rm it throws an error: missing operand.
        - if the user does mkdir dir1/dir2 and dir1 doesn't exist it will show an error: cannot make directory.
        - if the user does mkdir hello and hello exits it will again give the same error: cannot make directory.

date:

    flags used: -u, -R 
    Errors: None

cd: 
    flags used: --help, -P  
    Errors: None

pwd:
    flags used: --help, -P 
    Errors: None

history:
    flags used: -a, -c 
    Erros: None
